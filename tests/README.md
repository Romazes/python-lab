# Tests

This directory contains unit tests, integration tests, and test utilities.

## Purpose

- Ensure code quality and correctness
- Validate functionality across the repository
- Prevent regressions

## Usage

Tests should follow naming conventions (e.g., `test_*.py`) and use standard testing frameworks like `pytest` or `unittest`.

Run tests with:
```bash
pytest tests/
```
